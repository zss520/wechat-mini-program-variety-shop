.seg { display:flex; margin:24rpx; }
.seg text { flex:1; text-align:center; padding:16rpx; background:#fff; }
.seg .on { color:#c2410c; font-weight:700; }
.card { background:#fff; margin:16rpx 24rpx; padding:24rpx; border-radius:12rpx; }
.btn-primary { margin:48rpx 24rpx; }
