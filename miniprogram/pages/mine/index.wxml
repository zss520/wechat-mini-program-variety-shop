<view class="head" bindtap="login">{{user.nickname || '点击登录'}}</view>
<view class="card" bindtap="orders">我的订单</view>
<view class="card" bindtap="coupons">优惠券</view>
<view class="card" bindtap="points">我的积分</view>
<view class="card" bindtap="groups">拼团活动</view>
<view class="card" bindtap="seckill">限时秒杀</view>
<view class="card" bindtap="addr">收货地址</view>
<view class="card" bindtap="sub">备货通知 {{subscribed ? '已开启' : '未开启'}}</view>
<view class="card" bindtap="call">联系店主 {{settings.phone}}</view>
<view class="card" bindtap="privacy">隐私政策</view>
