<view class="wrap">
  <t-side-bar value="{{catId}}" bind:change="onCat">
    <t-side-bar-item label="全部" value="{{0}}" />
    <t-side-bar-item wx:for="{{cats}}" wx:key="id" label="{{item.name}}" value="{{item.id}}" />
  </t-side-bar>
  <view class="right">
    <view class="sorts">
      <t-segmented value="{{sort}}" options="{{sortOptions}}" bind:change="onSort" />
    </view>
    <scroll-view scroll-y class="list">
      <view class="goods-grid">
        <view class="goods-grid-item" wx:for="{{list}}" wx:key="id">
          <goods-card item="{{item}}" slot-id="category_list" position="{{index+1}}" />
        </view>
      </view>
      <t-empty wx:if="{{!list.length}}" icon="search" description="该分类暂无商品" />
    </scroll-view>
  </view>
</view>
